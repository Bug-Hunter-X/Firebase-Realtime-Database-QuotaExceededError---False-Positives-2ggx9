The Firebase Realtime Database sometimes throws a `QuotaExceededError` even when the app doesn't seem to be exceeding any quotas.  This can manifest as seemingly random failures to write data.  The error message itself isn't always descriptive enough to pinpoint the root cause.